"""LangGraph state machine wiring the LLM to the cost tools.

Build step 4 constructs the graph (reasoning node + tool node) using get_cost_tools().
Stub this session.
"""

from __future__ import annotations

from typing import Any


def build_agent_graph() -> Any:
    """Construct and return the compiled LangGraph agent. Build step 4."""
    raise NotImplementedError("LangGraph agent wiring lands in build step 4.")
