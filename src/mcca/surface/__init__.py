"""Surface layer: human-facing outputs (a self-contained HTML spend report).

All figures come from the deterministic query layer and the forecaster — never an LLM.
Depends on queries + forecasting only.
"""
